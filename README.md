# Fire Detection App (YOLO + Flask + Tkinter)

## Structure
- `backend/` → Flask API + YOLO model
- `frontend/` → Tkinter GUI client
- `requirements.txt` → Dependencies

## Run Instructions
1. Install dependencies  
   ```bash
   pip install -r requirements.txt
